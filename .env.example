# =====================
# Global environment
# =====================
# Change all secrets in real deployments. This file documents required vars.

# Postgres
POSTGRES_USER=hpulse
POSTGRES_PASSWORD=hpulse_password
POSTGRES_DB=hpulse
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg2://hpulse:hpulse_password@postgres:5432/hpulse

# Redis
REDIS_URL=redis://redis:6379/0

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
LOG_LEVEL=info
ENV=development

# Security
JWT_SECRET=change_me_in_production
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60
RATE_LIMIT_PER_MINUTE=120
CORS_ALLOW_ORIGINS=*

# Admin bootstrap (used by scripts/seed.sh)
ADMIN_EMAIL=admin@hpulse.local
ADMIN_PASSWORD=Admin123!#
ADMIN_ROLE=admin

# OpenTelemetry / Observability
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_SERVICE_NAME=hpulse-backend
PROMETHEUS_METRICS_PORT=9000

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Docker image tags (used by CI/CD)
IMAGE_REGISTRY=ghcr.io
IMAGE_NAMESPACE=your-github-username-or-org
BACKEND_IMAGE_NAME=hpulse-backend
FRONTEND_IMAGE_NAME=hpulse-frontend
IMAGE_TAG=latest